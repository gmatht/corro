//! Internal aliases for concrete rswidgets types so the shared GUI modules
//! stay written against local names rather than the concrete library types.
#[allow(dead_code)] // only used by the gui feature
pub type App = rswidgets::App;

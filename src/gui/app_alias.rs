//! Internal aliases for concrete rustxwidgets types so the shared GUI modules
//! stay written against local names rather than the concrete library types.
pub type App = rustxwidgets::App;
pub type Window = rustxwidgets::Window;

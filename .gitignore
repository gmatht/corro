/target
# Local integration tests (not for CI; copy template from repo if needed)
tests/local_*.rs
tmp*
# test_rec5.corro is now tracked in git (committed as canonical test data).

gtk-dlopen
__pycache__/
*.pyc
junk.*
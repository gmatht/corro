/target
# Alternate CARGO_TARGET_DIR build trees (size/PGO comparisons) and the
# scratch documents/logs written by scripts/sizes.sh and ad-hoc probing.
/target-o3
/target-oz
/target-pgso
/size.log
/size.log.bak
/tmux-client-*.log
/tt.csv
/tt.tsv
/tt.txt
# Stale full-tree copy left by an earlier refactor; the real tree is src/.
/corro.src/
/saved/
/run
/test_explicit
/AB.corro
/foo3.corro
/Sheet1.tsv
# Local integration tests (not for CI; copy template from repo if needed)
tests/local_*.rs
tmp*
# Win95/QEMU build artifacts + scratch VM screenshots
dist/
# PIR agent internal session state
.pir/
.resume/
scripts/__pycache__/
# test_rec5.corro is generated by gui_loop.sh; never commit.
/test_rec5.corro

gtk-dlopen
__pycache__/
*.pyc
junk.*
debug.log
stderr*.txt
stdout*.txt
commit_msg.txt
# Stray test artifacts
/null
/t.corro
/--

# Compiled shim objects (regenerated by tools/make-armstubs.sh)
tools/*.o

/target
# Local integration tests (not for CI; copy template from repo if needed)
tests/local_*.rs
tmp*
# Win95/QEMU build artifacts + scratch VM screenshots
dist/
# PIR agent internal session state
.pir/
.resume/
scripts/__pycache__/
